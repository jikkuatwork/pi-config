# Framework7 Router

Use this index as the first loaded reference for Framework7 tasks.

## Operating rule

Build no-build Framework7 v9 + Vue 3 apps only: no npm, Vite, Webpack, SFCs, package installs, or generated build config unless explicitly requested.

## Route

- For all Framework7 implementation tasks, read `FRAMEWORK7_VUE_NO_BUILD.md` before coding; follow its copy/paste boilerplate and final checklist.
- For Holm/Zippy/BFBB work, also read `HOLM_BFBB_ORIENTATION.md` and keep Holm details isolated there.
- For trigger review/skill edits, read `EVALS.md`.

## Output contract

Output runnable browser files plus a short smoke-test checklist.
